# This directory stores release packages
# Release packages are saved here during the build process and committed to the repository
# This allows the publish workflow to read packages locally before attempting to download from GitHub releases
